export interface Product {
    id: number;
    name: string;
    description: string;
    quantity: number;
}

export interface Customer {
    id: number;
    name: string;
    lastName: string;
    phoneNumber: string;
    email: string;
}

